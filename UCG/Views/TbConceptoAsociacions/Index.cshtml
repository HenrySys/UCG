@model IEnumerable<UCG.Models.TbConceptoAsociacion>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdAsociacionNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdConceptoNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdAsociacionNavigation.IdAsociacion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdConceptoNavigation.IdConceptoMovimiento)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdConceptoAsociacion">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdConceptoAsociacion">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdConceptoAsociacion">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
