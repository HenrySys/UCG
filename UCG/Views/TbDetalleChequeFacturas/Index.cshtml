@model IEnumerable<UCG.Models.TbDetalleChequeFactura>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Monto)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Observacion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdAcuerdoNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdChequeNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdFacturaNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdMovimientoEgresoNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Monto)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Observacion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdAcuerdoNavigation.IdAcuerdo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdChequeNavigation.IdCheque)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdFacturaNavigation.IdFactura)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdMovimientoEgresoNavigation.IdMovimientoEgreso)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdDetalleChequeFactura">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdDetalleChequeFactura">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdDetalleChequeFactura">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
